fastapi
uvicorn[standard]
pandas
numpy<2  # pinned: scikit-learn-extra (k-medoids) is not importable under numpy 2.x (ABI)
scipy
scikit-learn
scikit-learn-extra
mlxtend
statsmodels
matplotlib
seaborn
catboost
lightgbm
xgboost
shap
# tensorflow-cpu, not tensorflow: the plain wheel is 621 MB against 247 MB
# because it ships GPU kernels, and Cloud Run has no GPU to run them on. Same
# import surface — lstm_forecasting_analysis.py's `import tensorflow as tf` and
# `from tensorflow.keras...` are unchanged.
tensorflow-cpu
umap-learn
pingouin
linearmodels
plotly
pillow
PyPortfolioOpt
QuantLib
empyrical-reloaded
pytz
arch
lifetimes
pulp
