# relative-importance (Python) — Validation Report
- **Target**: `relative_importance_analysis.py` · Backend: `scottierieh/backend` (Python)
- **Method**: Ran the CLI script; reproduced its computation with **statsmodels OLS semi-partial R² relative weights** and compared the key result.
## Summary — pass
Result matches a direct reproduction with statsmodels OLS semi-partial R² relative weights. Repro: `validation/validate_relative_importance.py`.
